const styleCSS =
  "text-white bg-gradient-to-r from-green-500 via-green-600 to-green-700  hover:bg-green-600 hover:text-white";

const activePage = window.location.pathname;
const navLinks = document.querySelectorAll("nav a").forEach((link) => {

  
});
